# React + TypeScript + Vite

This directory contains the React web client for multi-agent-s2c.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

The Vite dev server proxies `/api` requests to `http://localhost:5050`.
